// Package hooks runs user-configured external commands (see the
// restore-hooks capability) sequentially, streaming their output live and
// reporting per-command success or failure without aborting the run.
package hooks
