// Package cmd implements the mumu CLI: save, restore, show, list, and
// delete window-to-Space layouts, plus a permission status check. All
// commands are top-level; there is no subcommand grouping.
package cmd
